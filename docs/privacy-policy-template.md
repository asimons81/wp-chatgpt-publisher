# Privacy policy template

Replace bracketed values and obtain appropriate legal review before publication.

**Service:** [Operator] runs Editorial Publisher for ChatGPT at [service URL] to connect users' ChatGPT sessions to WordPress sites they explicitly authorize.

**Data processed:** connection/site metadata, approving WordPress user identifier/display name, approved scopes, encrypted integration credentials, OAuth token hashes/metadata, minimal operation/audit metadata, error category, timing, and request identifiers. WordPress content and files may transit service memory to fulfill a user request but are not persisted by default.

**Not collected by default:** ChatGPT prompts/conversations, article or draft bodies at rest, uploaded files at rest, primary WordPress passwords, authorization headers, or invasive analytics. Telemetry is disabled by default.

**Purpose:** authenticate connections, perform requested editorial operations, prevent replay/abuse, diagnose failures, and maintain security.

**Retention:** [state code/grant/token/idempotency/log/database retention]. Expired temporary records are deleted automatically where implemented. Backups expire after [period].

**Sharing:** data is sent only between ChatGPT/OpenAI, the operator's MCP service, the user's authorized WordPress site, and infrastructure processors listed at [subprocessor URL].

**User controls:** revoke in WordPress and ChatGPT; request export/deletion at [contact]. WordPress content remains governed by the site's own policies.

**Security/contact:** [security measures and private contact]. **Last updated:** [date].
